## INSTRUCTIONS: Make a copy of this file named "docker-compose.actual.yml" and fill in the Actual values.
##
# This is a Shadow file, illustrating what the actual file should contain.
# 
# Docker Compose file for Dockhand with PostgreSQL
services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres_01
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: dockhand
      POSTGRES_PASSWORD: ChangeMe
      POSTGRES_DB: dockhand
    volumes:
      - postgres_data:/var/lib/postgresql/data

  dockhand:
    image: fnsys/dockhand:latest
    container_name: dockhand_01
    restart: always
    ports:
      - 3000:3000
    environment:
      DATABASE_URL: postgres://dockhand:ChangeMe@postgres:5432/dockhand
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - dockhand_data:/app/data
    depends_on:
      - postgres

volumes:
  postgres_data:
  dockhand_data: